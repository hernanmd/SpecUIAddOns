Allows to specify the file extension string. Use only the extension characters, for example 'csv' or 'txt', not '.csv' or '.doc'.

SpcLabeledTextFieldWithBrowseButton new 
	ghostText: 'Please enter a value';
	label: 'File name';
	fileExtension: 'csv';
	openWithSpec.
